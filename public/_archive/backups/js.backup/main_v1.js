// Main Entry Point
// Imports all modules to ensure they are executed and registered

// Common module (Firebase) must be imported as module
import './common_v1.js';

// Other scripts are now loaded via <script> tags in index.html
// to ensure global scope availability for onclick handlers.

console.log("Main script loaded.");
