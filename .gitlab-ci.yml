image: node:lts  # Node.js 환경 설정

pages:
  script:
    - npm install
    - npm run build
    - mkdir -p public
    - cp -r * public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH